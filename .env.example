# HTTP port used by the application
PORT=3000

# Optional path to the SQLite database file
DATABASE_PATH=./data/helloword.sqlite

# Set to false to start without the sample workspace records
SEED_DEMO_DATA=true